# Optional extras. None of these are needed to run the prototype.

# Real model providers (only used when an API key is configured).
openai>=1.54
anthropic>=0.39

# PII_ENGINE=hybrid layers Microsoft Presidio's named-entity recognition on top
# of the built-in deterministic recognizers. Note: importing the analyzer
# downloads a ~400MB spaCy model on first use.
presidio-analyzer>=2.2
presidio-anonymizer>=2.2

# Postgres and Redis backends, for running the same code beyond one laptop.
asyncpg>=0.30
redis>=5.2

# Tests.
pytest>=8.3
pytest-asyncio>=0.24
